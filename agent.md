# Agent Notes

## Development
- Run tests with `cargo test`.
- Format with `cargo fmt`.
- Lint with `cargo clippy`.
- For releases, the project uses `cargo release` to bump the crate version and publish to crates.io.
- Do not manually change the package version in `Cargo.toml` or `Cargo.lock` when preparing changelog entries or release notes.
