# Agent Notes

## Development
- Run tests with `cargo test`.
- Format with `cargo fmt`.
- Lint with `cargo clippy`.
- For releases, the project uses `cargo publish` to bump the crate version and publish to crates.io.
